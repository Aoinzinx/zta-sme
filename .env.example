# .env.example — NEVER commit actual values to version control
DATABASE_URL=postgresql://ztuser:CHANGEME@localhost:5432/ztdb
JWT_SECRET=<256-bit random secret from AWS Secrets Manager>
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_HOURS=8
POLICY_ENGINE_URL=http://127.0.0.1:8001
UPSTREAM_AWS_URL=https://<api-gateway-id>.execute-api.<region>.amazonaws.com/prod
UPSTREAM_AZURE_URL=https://<app-service-name>.azurewebsites.net
ENVIRONMENT=production
